"""Concrete LLM provider adapters (openai_compat / anthropic / bedrock / vllm).

Providers live *inside* the ``llm`` package — not as a top-level distribution —
to mirror litellm / instructor / dspy / llama-index conventions.
"""

__all__: list[str] = []
